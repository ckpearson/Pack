<UserControl x:Class="Pack_v2.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pack_v2.Views"
             xmlns:viewModels="clr-namespace:Pack_v2.ViewModels"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:ProgressViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <controls:MetroProgressBar Grid.Row="1" IsIndeterminate="{Binding IsIndeterminate, Mode=OneWay}"
                                       Value="{Binding Progress, Mode=OneWay}" />
        
        <TextBlock Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Stretch"
                   Margin="0,5,0,0"
                   FontSize="14"
                   TextAlignment="Center" TextWrapping="Wrap" Text="{Binding Message, Mode=OneWay}" />
    </Grid>
</UserControl>
